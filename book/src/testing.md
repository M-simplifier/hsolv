# テスト戦略

対応コード: `test/Main.hs`

テストは「ユニット + PBT (QuickCheck)」の二段構えです。

ユニットテスト:
- `simplify` の具体例
- `solve` の典型ケース
- パースや表示の確認

PBT の狙い:
- `simplify` が評価値を保つ
- `pretty` の往復で構造が崩れない
- 二次方程式の解が元の式を満たす

ユニットと PBT を組み合わせることで、想定外の入力にも耐える設計になります。
