# 拡張の指針

次のような拡張が比較的安全に追加できます。

- 追加の関数: `min`, `max`, `sinh` など
- 簡約ルールの拡充: 係数の整理や因数分解
- 複素数対応: `i` を導入して `solve` の範囲を拡張
- コマンド拡張: `:let` による環境保存

拡張時は次を守ると安全です。

1. AST に新コンストラクタを追加
2. パーサ/プリンタ/評価/簡約/テストを揃える
3. UI には結果の表示だけ追加する
