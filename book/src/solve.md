# 方程式ソルバ

対応コード: `src/HSolv/Solve.hs`

現状のソルバは「一次・二次の実数解」に限定しています。目的は学習用に範囲を明確化し、堅牢にすることです。

仕様:
- 形式: `solve x <expr>` は `<expr> = 0` を解く
- 係数は有理数で扱う
- 判別式が負の場合はエラー（実数解なし）

二次方程式:

```
ax^2 + bx + c = 0
```

判別式 `D = b^2 - 4ac` を使って解を構成します。`D` が平方数なら `sqrt` を含まない有理数解を返します。
